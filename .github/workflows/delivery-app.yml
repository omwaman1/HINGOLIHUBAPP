name: Build Delivery App APK & AAB

on:
  push:
    tags:
      - 'delivery-v*'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
    
    - name: Grant execute permission for gradlew
      working-directory: delivery-app
      run: chmod +x gradlew
    
    - name: Build Debug APK
      working-directory: delivery-app
      run: ./gradlew assembleDebug
    
    - name: Build Signed Release APK
      working-directory: delivery-app
      run: ./gradlew assembleRelease
    
    - name: Build Signed Release AAB (App Bundle)
      working-directory: delivery-app
      run: ./gradlew bundleRelease
    
    - name: Get version name
      id: get_version
      run: echo "VERSION=${GITHUB_REF#refs/tags/delivery-}" >> $GITHUB_OUTPUT
    
    - name: Rename build outputs with version
      run: |
        mv delivery-app/app/build/outputs/apk/debug/app-debug.apk delivery-app/app/build/outputs/apk/debug/DeliveryApp-${{ steps.get_version.outputs.VERSION }}-debug.apk
        mv delivery-app/app/build/outputs/apk/release/app-release.apk delivery-app/app/build/outputs/apk/release/DeliveryApp-${{ steps.get_version.outputs.VERSION }}-release.apk
        mv delivery-app/app/build/outputs/bundle/release/app-release.aab delivery-app/app/build/outputs/bundle/release/DeliveryApp-${{ steps.get_version.outputs.VERSION }}-release.aab
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        name: Delivery App ${{ steps.get_version.outputs.VERSION }}
        tag_name: ${{ github.ref_name }}
        draft: false
        prerelease: false
        files: |
          delivery-app/app/build/outputs/apk/debug/DeliveryApp-${{ steps.get_version.outputs.VERSION }}-debug.apk
          delivery-app/app/build/outputs/apk/release/DeliveryApp-${{ steps.get_version.outputs.VERSION }}-release.apk
          delivery-app/app/build/outputs/bundle/release/DeliveryApp-${{ steps.get_version.outputs.VERSION }}-release.aab
        generate_release_notes: true
        body: |
          ## Delivery App ${{ steps.get_version.outputs.VERSION }}
          
          ### Downloads
          - **Release APK** (Signed, for direct install): `DeliveryApp-${{ steps.get_version.outputs.VERSION }}-release.apk`
          - **Release AAB** (Signed, for Play Store): `DeliveryApp-${{ steps.get_version.outputs.VERSION }}-release.aab`
          - **Debug APK** (For testing): `DeliveryApp-${{ steps.get_version.outputs.VERSION }}-debug.apk`
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
